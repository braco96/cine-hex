package com.braco96.cine.infrastructure.persistence.jpa; import org.springframework.data.jpa.repository.JpaRepository; public interface SpringCuentaRepository extends JpaRepository<JpaCuenta,Long>{ JpaCuenta findByClienteId(Long clienteId); }
